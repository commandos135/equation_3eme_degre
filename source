#include "stdafx.h"
#include <iostream> 
#include <math.h> 
using namespace std;

int main()
{
	int x,Y;
 
	for (int i = 0; i < 11; i++)

	{
	Y = ((-i*i*i) + (i*i) + (5*i)-7);
		cout << "y(" << i <<") =" << Y << endl;
	}
	system("pause");
	return 0;
  
}
